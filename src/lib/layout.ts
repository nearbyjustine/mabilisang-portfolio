/**
 * One measure for the whole page. Nav, sections, and footer share it so every
 * left edge on the page lines up, at every breakpoint.
 */
export const container = "mx-auto w-full max-w-6xl px-6 md:px-10 lg:px-12";

/** Vertical rhythm for a major section. */
export const sectionPadding = "py-24 md:py-32 lg:py-36";
